import styled from "styled-components";

export const LogoText = styled.h1`
    color:#fff;
    cursor:pointer;
    transition: all 0.2s ease;
img{
    width:50px;
    /* height:60px; */
    color:#fff;
    
}
    &:hover{
        transition: all 0.2s ease;
        transform: scale(1.1);
    }
`